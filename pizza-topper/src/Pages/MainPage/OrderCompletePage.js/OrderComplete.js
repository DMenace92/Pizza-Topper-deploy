import react from "react";
import Styles from "./OrderComplete.module.css";

const OrderComplete = () => {
  return <div className={Styles.testText}>this is the order page</div>;
};

export default OrderComplete;
